<!--pages/home/childrenCpts/my-goods-item/my-goods-item.wxml-->
<navigator url="/pages/goodsDetail/goodsDetail?iid={{goodsItem.iid || goodsItem.item_id}}" class="goods-item">
  <image class="image" src="{{goodsItem.show.img || goodsItem.image}}" mode="widthFix"></image>
  <view class="desc">
    <view class="title">{{goodsItem.title}}</view>
    <view>
      <text class="price">￥{{goodsItem.price}}</text>
      <image class="cfav-img" src="/assets/common/favor.png"></image>
      <text class="cfav">{{goodsItem.cfav}}</text>
    </view>
  </view>
</navigator>
